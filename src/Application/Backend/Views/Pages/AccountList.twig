{% extends 'Backend/Views/BackendLayout.twig' %}
{% expect 'App\\Application\\Backend\\Views\\Pages\\AccountList' as MODEL %}


{% block PAGE_TITLE %}Accounts{% endblock %}

{% block PAGE_H1 %}Accounts <small>List of users</small>{% endblock %}


{% block PAGE_CONTENT %}
    <section>
        <h1>Accounts</h1>
        
        <table class="table">
            <thead>
                <tr>
                    <th style="width: 30px">#</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Id</th>
                </tr>
            </thead>
            <tbody>
                {% for id, account in MODEL.Accounts %}
                <tr>
                    <td>{{ id }}</td>
                    <td class="table--text-main"><a href="{{ path('backend_account_show', { accountId:account.Id }) }}">{{ account.Forename ~ ' ' ~ account.Lastname }}</a></td>
                    <td class="">{{ account.Email }}</td>
                    <td class="">{{ account.Id }}</td>
                </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center">No result</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </section>
    
{% endblock %}
